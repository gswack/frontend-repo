name: Frontend Docker Build and Push

on:
  push:
    branches:
      - dev

jobs:
  build-and-push:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get Image Tag
        id: image
        shell: powershell
        run: |
          "tag=${{ github.sha }}" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Login to Nexus Docker Registry
        run: docker login "${{ secrets.NEXUS_REGISTRY }}" -u "${{ secrets.NEXUS_USERNAME }}" -p "${{ secrets.NEXUS_PASSWORD }}"

      - name: Build Docker image
        run: |
          docker build -t ${{ secrets.NEXUS_REGISTRY }}/frontend:v6 .

          
      - name: Push Docker image
        run: |
          docker push ${{ secrets.NEXUS_REGISTRY }}/frontend:v6